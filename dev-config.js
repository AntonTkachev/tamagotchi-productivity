// Local development config. Change DEV_MODE to true to enable the dev panel
// in Settings. Run: git update-index --assume-unchanged dev-config.js
// so this change is never accidentally committed or shipped.
const DEV_MODE = false;
